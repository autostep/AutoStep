namespace AutoStep.Execution
{
    public class InteractionVariables : VariableSetBase<object>
    {
        public InteractionVariables(bool isReadOnly = false) 
            : base(isReadOnly)
        {
        }
    }
}
